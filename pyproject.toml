[project]
name = "enterprise-operations-ai-agent"
version = "1.0.0"
requires-python = ">=3.13,<3.14"
dependencies = []

[tool.fastapi]
entrypoint = "backend.app.main:app"

[tool.setuptools]
py-modules = []